{
  "name": "ecowas-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd apps/web && node_modules/.bin/vite",
    "build": "cd apps/web && node_modules/.bin/vite build",
    "preview": "cd apps/web && node_modules/.bin/vite preview"
  }
}